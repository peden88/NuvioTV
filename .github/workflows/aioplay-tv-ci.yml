name: AIOPlay TV CI

on:
  push:
    branches:
      - feature/aioplay-tv
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: aioplay-tv-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build AIOPlay TV debug APK
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Create CI properties
        run: |
          printf '%s\n' \
            'NUVIO_SUPABASE_URL=https://ci.invalid.example' \
            'NUVIO_SUPABASE_ANON_KEY=ci-placeholder' \
            'AIOPLAY_API_BASE_URL=https://aioplay-ci.invalid.example' \
            > local.properties

      - name: Build AIOPlay TV
        run: ./gradlew :app:assembleAioplayDebug --stacktrace --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: aioplay-tv-debug-${{ github.sha }}
          path: app/build/outputs/apk/aioplay/debug/*.apk
          if-no-files-found: error
          retention-days: 7
